# Modernization Risk Register

Scoring: likelihood 1-5, impact 1-5, score = likelihood x impact.

| Score | Severity |
| --- | --- |
| 20-25 | critical |
| 12-19 | high |
| 6-11 | medium |
| 1-5 | low |

| Risk ID | Description | Likelihood | Impact | Score | Severity | Owner | Linked WP | Mitigation | Trigger/Signal | Status |
| --- | --- | ---: | ---: | ---: | --- | --- | --- | --- | --- | --- |
| RISK-001 | `server/tests/bugfixes.test.js` "forwards ordinary Say chat" has a hard 5000 ms timeout and fails on a **cold** first run of the full server suite, then passes warm. Observed twice on the unmodified baseline (`8642dc4`): cold run 534/535 exit 1 with import cost 86.13 s; warm run 535/535 exit 0 with import cost 35.40 s; the file alone passes 93/93 in 3.27 s with import 1.39 s. The baseline is green but **not deterministic on first run**. | 4 | 4 | 16 | high | coordinator | FND-001 (found), FND-002 (gate consumer) | Record both runs in every baseline gate rather than the passing one. Any agent seeing this single failure must re-run before concluding a regression. Permanent fix is a raised per-test timeout or a warmed import, which changes test files and therefore belongs to a package that owns them — **not** FND-001, whose contract forbids source changes. | A red server suite whose only failure is this test, especially on a fresh checkout or clean CI runner | open |
| RISK-002 | `data/db.example.json` is stale against the authoritative schema in `server/database/init.js` `defaultData` (lines 57-76). It carries 3 keys the schema does not define (`activity_log`, `mods`, `active_server_id`) and omits 14 that it does. | 3 | 4 | 12 | high | coordinator | DB-001, DB-003 | Treat `defaultData` (19 collections) as the only schema authority. Derive all synthetic fixtures from it. Do not seed the importer or parity comparator from the example file. The plan's V1 Collection Mapping table already matches `defaultData` exactly, so the plan is right and the example file is wrong. | Any fixture, importer test, or parity report referencing `activity_log`, `mods`, or `active_server_id`, or covering fewer than 19 collections | open |
| RISK-003 | The FND-001 performance baseline covers exactly one route: `GET /api/auth/status`, unauthenticated and non-I/O. Authenticated, filesystem, RCON, and PanelBridge routes have no recorded p50/p95. | 4 | 3 | 12 | high | coordinator | FND-001, FND-002 | Anchor only the plan's "contract validation adds no more than 5 ms p95" budget to this number. Before any lifecycle or persistence work claims a performance comparison, extend `measure-baseline.mjs` coverage to representative authenticated and I/O-bound routes. State the gap rather than implying broad coverage. | Any claim that a change "did not regress performance" that cites only PERF.json | open |
| RISK-004 | Client production build emits a >500 kB post-minification chunk advisory, and no byte figure has been recorded. The plan requires per-phase measurement of English bundle growth. | 3 | 2 | 6 | medium | coordinator | I18N-001 | Record the actual `client/dist` byte size before locale resources are introduced, so growth is measured against a real number rather than an advisory string. | Locale namespaces added with no prior recorded bundle size | open |
| RISK-005 | Worktrees are prohibited until a user-authorized local checkpoint commit exists, because untracked handoff files do not appear in a new worktree. Until then FND-002, FND-003, and DB-001 cannot run in parallel and the program is serialized through the coordinator. | 5 | 2 | 10 | medium | coordinator | FND-002, FND-003, DB-001 | Request the checkpoint commit at the FND-001 boundary, which is exactly where the plan places it. Do not create a worktree, branch, or commit before that authorization. | Any attempt to run `create-worktree.ps1` while `git status` still shows the handoff files untracked | open |

| RISK-006 | The mandatory baseline gate `npm run test:server` writes `data/db.json`, `data/backups/*.json`, and `logs/` into the repository, and `bootstrap-plan.ps1` then throws `data/db.json must not exist in the modernization fork baseline.` **The plan's own documented FND-001 sequence cannot be run twice without a manual cleanup.** Hit for real during FND-001 and reproduced independently by the verifier. Content was the empty default DB (identical SHA-256 across all six generated JSON files); no data was at risk and no secret was written. | 5 | 3 | 15 | high | coordinator | FND-001 (found), FND-002 onward (every future gate) | Full analysis and four remediation options recorded as DISC-001 in `DECISIONS.md`; recommendation is to isolate the test suite's data root the way the perf step already isolates its own via `paths.config.json`. Interim: `README.md` documents the cleanup so the throw is self-explaining instead of alarming. **Awaiting user decision — not yet fixed.** | `bootstrap-plan.ps1` throwing on `data/db.json` immediately after a green gate | open |

Statuses: open, mitigating, accepted, resolved, superseded.
