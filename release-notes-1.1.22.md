## v1.1.22

### Fixed

- **First server startup**: the server-creation wizard now saves the configured admin password. New Project Zomboid servers receive `-adminpassword` on first launch instead of attempting an unavailable interactive password prompt and exiting.

---

### Downloads

- **ZomboidControlPanel-windows.zip**: Windows full package, extract and run `Start.bat`.
- **ZomboidControlPanel-linux.tar.gz**: Linux full package, extract and run `./start.sh`.
- **checksums.txt**: SHA256 verification hashes.